# TAT API
TAT_API_KEY=

# PostgreSQL — localhost because DB is on the same VM
MIGRATE_PG_HOST=localhost
MIGRATE_PG_PORT=5432
MIGRATE_PG_DB=taluithai
MIGRATE_PG_USER=postgres
MIGRATE_PG_PASSWORD=

# Prefect Cloud
PREFECT_API_KEY=
